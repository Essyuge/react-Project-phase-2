import React from 'react'

function Header() {
  return (
    <header>
    <h1>
        $$ BOOK STORE
        </h1>
        <nav>

        <button>Light Mode</button>
        </nav>
    </header>
   
  )
}

export default Header